namespace LobotJR.Trigger
{
    public class TriggerManager
    {
        // Triggers are actions the bot takes in response to public messages.
        // Different from commands in that they don't need the ! prefix and are only triggered by public chat.
    }
}
